services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: samanvay
      POSTGRES_USER: samanvay
      POSTGRES_PASSWORD: samanvay
    ports:
      - "5432:5432"
    volumes:
      - samanvay-postgres-data:/var/lib/postgresql/data

volumes:
  samanvay-postgres-data:
